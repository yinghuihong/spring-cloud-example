server:
  port: 8779

spring:
  application:
    name: config-client
  cloud:
    config:
      # 指明远程仓库的分支
      label: master
      # 开发环境配置文件
      profile: dev
      # 配置服务中心的网址
      uri: http://localhost:8771/
