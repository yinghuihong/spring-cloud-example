server:
  port: 8819

spring:
  application:
    name: admin-client
  boot:
    admin:
      client:
        url: http://localhost:8811

management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: ALWAYS